<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.androidhive.loginandregistration"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="23" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="info.androidhive.loginandregistration.app.AppController"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="info.androidhive.loginandregistration.activity.LoginActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="info.androidhive.loginandregistration.activity.RegisterActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name="info.androidhive.loginandregistration.activity.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop" />

        <!-- Dashboard Activity -->
        <activity android:name="info.androidhive.loginandregistration.activity.DashboardActivity" />

        <!-- News Feed Activity -->
        <activity android:name="info.androidhive.loginandregistration.activity.NewsFeedActivity" />

        <!-- Friends Activity -->
        <activity android:name="info.androidhive.loginandregistration.activity.FriendsActivity" />

        <!-- Messages Activity -->
        <activity android:name="info.androidhive.loginandregistration.activity.MessagesActivity" />

        <!-- Places Activity -->
        <activity android:name="info.androidhive.loginandregistration.activity.PlacesActivity" />

        <!-- Events Activity -->
        <activity android:name="info.androidhive.loginandregistration.activity.EventsActivity" />

        <!-- Photos Activity -->
        <activity android:name="info.androidhive.loginandregistration.activity.PhotosActivity" />
    </application>

</manifest>