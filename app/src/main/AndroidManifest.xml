<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.androidhive.loginandregistration"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".app.AppController"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.LoginActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.RegisterActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop" />

        <!-- Dashboard Activity -->
        <activity android:name=".activity.DashboardActivity"></activity>

        <!-- News Feed Activity -->
        <activity android:name=".activity.NewsFeedActivity" ></activity>

        <!-- Friends Activity -->
        <activity android:name=".activity.FriendsActivity" ></activity>

        <!-- Messages Activity -->
        <activity android:name=".activity.MessagesActivity" ></activity>

        <!-- Places Activity -->
        <activity android:name=".activity.PlacesActivity" ></activity>

        <!-- Events Activity -->
        <activity android:name=".activity.EventsActivity" ></activity>

        <!-- Photos Activity -->
        <activity android:name=".activity.PhotosActivity" ></activity>
    </application>

</manifest>